<style>
  /* Main content styles */
  .main-content {
    max-width: 800px;
    margin: 2rem auto;
    padding: 0 2rem;
    position: relative;
  }

  .content-card {
    background: rgba(245, 240, 232, 0.9); /* Semi-transparent parchment */
    backdrop-filter: blur(10px);
    border-radius: 12px;
    padding: 3rem;
    margin-bottom: 2rem;
    border: 2px solid #8b7355; /* Darker parchment border */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    position: relative;
  }

  /* Markdown content styling */
  .markdown-content {
    color: #e2e8f0;
  }

  .markdown-content h1 {
    font-size: 2.5rem;
    font-weight: 700;
    color: #f1f5f9;
    margin-bottom: 2rem;
    text-align: center;
    border-bottom: 2px solid #64748b;
    padding-bottom: 1rem;
  }

  .markdown-content h2 {
    font-size: 1.8rem;
    font-weight: 600;
    color: #3d2c1d; /* Dark brown text */
    margin: 2.5rem 0 1.5rem 0;
    border-left: 4px solid #8b7355; /* Darker parchment border */
    padding-left: 1rem;
  }

  .markdown-content h3 {
    font-size: 1.4rem;
    font-weight: 600;
    color: #3d2c1d; /* Dark brown text */
    margin: 2rem 0 1rem 0;
  }

  .markdown-content p {
    margin-bottom: 1.5rem;
    line-height: 1.7;
    color: #3d2c1d; /* Dark brown text */
  }

  .markdown-content blockquote {
    border-left: 4px solid #8b7355; /* Darker parchment border */
    padding-left: 1.5rem;
    margin: 2rem 0;
    font-style: italic;
    color: #3d2c1d; /* Dark brown text */
    background: rgba(212, 196, 168, 0.2); /* Semi-transparent aged parchment */
    padding: 1rem 1.5rem;
    border-radius: 0 8px 8px 0;
  }

  .markdown-content ul,
  .markdown-content ol {
    margin: 1.5rem 0;
    padding-left: 2rem;
  }

  .markdown-content li {
    margin-bottom: 0.5rem;
    color: #e2e8f0;
  }

  .markdown-content code {
    background: rgba(100, 116, 139, 0.2);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-family: 'Monaco', 'Menlo', monospace;
    font-size: 0.9em;
    color: #f1f5f9;
  }

  .markdown-content pre {
    background: rgba(15, 23, 42, 0.8);
    border: 1px solid #475569;
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  .markdown-content pre code {
    background: none;
    padding: 0;
    color: #e2e8f0;
  }

  .markdown-content a {
    color: #60a5fa;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .markdown-content a:hover {
    color: #93c5fd;
    text-decoration: underline;
  }

  .markdown-content hr {
    border: none;
    height: 1px;
    background: linear-gradient(90deg, transparent, #64748b, transparent);
    margin: 3rem 0;
  }

  .markdown-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
    background: rgba(30, 41, 59, 0.6);
    border-radius: 8px;
    overflow: hidden;
  }

  .markdown-content th,
  .markdown-content td {
    padding: 0.75rem 1rem;
    text-align: left;
    border-bottom: 1px solid #475569;
  }

  .markdown-content th {
    background: rgba(100, 116, 139, 0.3);
    font-weight: 600;
    color: #f1f5f9;
  }

  .markdown-content td {
    color: #e2e8f0;
  }

  @media (max-width: 768px) {
    .main-content {
      padding: 0 1rem;
      margin: 2rem auto;
    }

    .content-card {
      padding: 2rem 1.5rem;
    }

    .markdown-content h1 {
      font-size: 2rem;
    }

    .markdown-content h2 {
      font-size: 1.5rem;
    }
  }
</style>

<main class="main-content">
  <div class="content-card">
    <div class="markdown-content">
      <%= @content.html_safe %>
    </div>
  </div>
</main>
